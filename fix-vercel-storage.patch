diff --git a/its-ala/src/lib/postgres.ts b/its-ala/src/lib/postgres.ts
index 0000000..0000000 100644
--- a/its-ala/src/lib/postgres.ts
+++ b/its-ala/src/lib/postgres.ts
@@
-import { createClient } from "@vercel/postgres"; const connectionString = process.env.POSTGRES_URL; const client = createClient(connectionString ? { connectionString } : undefined); export function sql(strings: TemplateStringsArray, ...values: unknown[]) { return client.sql(strings, ...values); }
+import { createClient } from "@vercel/postgres";
+
+const connectionString = process.env.POSTGRES_URL;
+const client = createClient(connectionString ? { connectionString } : undefined);
+
+export type SqlPrimitive = string | number | boolean | null | undefined;
+
+export function sql(strings: TemplateStringsArray, ...values: SqlPrimitive[]) {
+  return client.sql(strings, ...values);
+}
diff --git a/its-ala/src/lib/workspace-store.ts b/its-ala/src/lib/workspace-store.ts
index 0000000..0000000 100644
--- a/its-ala/src/lib/workspace-store.ts
+++ b/its-ala/src/lib/workspace-store.ts
@@
-async function ensurePostgresSchema() {
+export async function ensureWorkspacePostgresSchema() {
   await sql` CREATE TABLE IF NOT EXISTS project_workspaces ( id TEXT PRIMARY KEY, lead_id TEXT NOT NULL REFERENCES inquiries(id) ON DELETE CASCADE, slug TEXT UNIQUE NOT NULL, client_name TEXT NOT NULL, client_email TEXT NOT NULL, company TEXT NOT NULL, title TEXT NOT NULL, status TEXT NOT NULL, overview TEXT NOT NULL, current_focus TEXT NOT NULL, next_step TEXT NOT NULL, communication_guidance TEXT NOT NULL, contact_email TEXT NOT NULL, access_code TEXT NOT NULL, is_active BOOLEAN NOT NULL DEFAULT TRUE, created_at TIMESTAMPTZ NOT NULL, updated_at TIMESTAMPTZ NOT NULL, milestones_json JSONB NOT NULL DEFAULT '[]'::jsonb, updates_json JSONB NOT NULL DEFAULT '[]'::jsonb, resources_json JSONB NOT NULL DEFAULT '[]'::jsonb ); `;
 }
+
+const ensurePostgresSchema = ensureWorkspacePostgresSchema;
diff --git a/its-ala/src/lib/upstream-store.ts b/its-ala/src/lib/upstream-store.ts
index 0000000..0000000 100644
--- a/its-ala/src/lib/upstream-store.ts
+++ b/its-ala/src/lib/upstream-store.ts
@@
-async function ensurePostgresSchema() {
+export async function ensureUpstreamPostgresSchema() {
   await sql` CREATE TABLE IF NOT EXISTS upstream_state ( id TEXT PRIMARY KEY, payload JSONB NOT NULL, updated_at TIMESTAMPTZ NOT NULL ); `;
 }
+
+const ensurePostgresSchema = ensureUpstreamPostgresSchema;
